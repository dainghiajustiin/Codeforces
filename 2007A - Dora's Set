#include<bits/stdc++.h>
using namespace std;

#define int long long
#define Mp make_pair
#define fi first
#define se second
#define pb push_back
#define el '\n'
#define all(x) x.begin(), x.end()

const int MOD = (int) 1e9 + 7;
const int inf = (int) 1e18 + 7;
using pii = pair<int,int>;

/// code
void solve(){
    int l, r;
    cin >> l >> r;
    int le1 = l/2*2 + 1;
    int le2 = le1 + 2;
    int ans = 0;
    for(int c = l/2*2; c <= r && le2 <= r; c += 2){
        ans++;
        le1 += 4;
        le2 += 4;
    }

    cout << ans << el;
}

signed main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);

//    #define task ""
//    freopen(task".inp", "r", stdin);
//    freopen(task".out", "w", stdout);

    int t; cin >> t;
    while(t--) solve();

    return 0;
}
///      Today is 30.08.2024
///    Do your best because tomorrow will be better!
///                                sign
///                               Justiin
